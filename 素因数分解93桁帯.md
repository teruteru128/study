## 93桁帯素因数分解

- factordbから’C’を取得
- 試し割り等はパス。どうせでかい合成数から割り出された数が対象だし
- ECM(30桁ぐらいまで)を数分計算する
- factordbでのIDを取得(`curl -X GET --cookie "fdbuser=410c610cccf69f308fd32aba309579a3"  https:// factordb.com/api?id=<id>`形式、最初はIDを持っていないため)
- 隠しエンドポイントでSNFS用多項式の存在をチェック
- if(has(多項式))
  - あり: SNFS
  - なし: スキップ。ブラックリストへ登録する

こんな感じですかね？

