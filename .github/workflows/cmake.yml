name: cmake

on: [push]

jobs:
  build:
    name:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2.3.4
        with:
          submodules: 'recursive'
      - name: Install dependents
        run: |
          sudo apt install autopoint libcurl4-gnutls-dev libgmp-dev libsodium-dev libupnp-dev libxmlrpc-core-c3-dev uuid-dev libcunit1-dev libjson-c-dev libtommath-dev libtomcrypt-dev mingw-w64 ninja-build
      - name: Make dir
        run: mkdir build
      - name: Generate configure
        run: cmake --no-warn-unused-cli -DCMAKE_EXPORT_COMPILE_COMMANDS:BOOL=TRUE -DCMAKE_BUILD_TYPE:STRING=Debug -H${{ github.workspace }} -G Ninja
        working-directory: ./build
      - name: Make
        run: ninja -j
        working-directory: ./build
      - name: output log on fail
        if: failure()
        run: cat ${{ github.workspace }}/build/CMakeFiles/CMakeOutput.log
